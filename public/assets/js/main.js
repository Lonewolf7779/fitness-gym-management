/**
 * IRONCORE Core Application Script
 * Handles Navigation, Sticky Header, Mobile Drawer, Accessibility & live dashboards.
 */
document.addEventListener('DOMContentLoaded', () => {
  const navbar=document.querySelector('.navbar'), mobileToggle=document.querySelector('.mobile-toggle'), navMenu=document.querySelector('.nav-menu');
  window.addEventListener('scroll',()=>window.scrollY>20?navbar?.classList.add('scrolled'):navbar?.classList.remove('scrolled'));
  if(mobileToggle&&navMenu){mobileToggle.addEventListener('click',()=>{const isOpen=navMenu.classList.toggle('is-active');mobileToggle.classList.toggle('is-active');mobileToggle.setAttribute('aria-expanded',isOpen?'true':'false');document.body.style.overflow=isOpen?'hidden':'';});navMenu.querySelectorAll('.nav-link').forEach(link=>link.addEventListener('click',()=>{navMenu.classList.remove('is-active');mobileToggle.classList.remove('is-active');mobileToggle.setAttribute('aria-expanded','false');document.body.style.overflow='';}));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&navMenu.classList.contains('is-active')){navMenu.classList.remove('is-active');mobileToggle.classList.remove('is-active');mobileToggle.setAttribute('aria-expanded','false');document.body.style.overflow='';}});}
  document.querySelectorAll('a[href^="#"]').forEach(anchor=>anchor.addEventListener('click',function(e){const targetId=this.getAttribute('href');if(targetId==='#')return;const targetEl=document.querySelector(targetId);if(targetEl){e.preventDefault();window.scrollTo({top:targetEl.getBoundingClientRect().top+window.pageYOffset-80,behavior:'smooth'});}}));
  loadLiveDashboard();
});
async function loadLiveDashboard(){if(!document.querySelector('.kpi-grid,.dashboard-shell'))return;try{const response=await fetch('/dashboard-data.php',{credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json'}});if(!response.ok)return;const data=await response.json();if(!data.success)return;if(data.role==='admin'){updateAdminDashboard(data);loadLiveAdminChart();}if(data.role==='trainer')updateTrainerDashboard(data);if(data.role==='member')updateMemberDashboard(data);document.documentElement.dataset.liveDashboard='true';}catch(error){console.warn('IRONCORE live dashboard unavailable:',error);}}
function normalizeLabel(s){return String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,'');}
function setKpiByTitle(title,value){const wanted=normalizeLabel(title);document.querySelectorAll('.kpi-card').forEach(card=>{const label=card.querySelector('.kpi-title');if(!label)return;const actual=normalizeLabel(label.textContent);if(actual===wanted||actual.includes(wanted)||wanted.includes(actual)){const valueEl=card.querySelector('.kpi-value');if(valueEl)valueEl.textContent=value;}});}
function initials(name){return String(name||'').trim().split(/\s+/).slice(0,2).map(x=>x[0]||'').join('').toUpperCase();}
function escapeHtml(value){return String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function formatDate(value){if(!value)return'—';const d=new Date(`${value}T00:00:00`);return Number.isNaN(d.getTime())?value:d.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'});}
function updateAdminDashboard(data){const s=data.stats||{};setKpiByTitle('Total Members',Number(s.total_members||0).toLocaleString('en-IN'));setKpiByTitle('Active Members',Number(s.active_members||0).toLocaleString('en-IN'));setKpiByTitle("Today's Attendance",Number(s.today_attendance||0).toLocaleString('en-IN'));setKpiByTitle('Monthly Revenue',`₹${Number(s.monthly_revenue||0).toLocaleString('en-IN',{maximumFractionDigits:0})}`);const tables=document.querySelectorAll('.panel-card .data-table tbody'),recentBody=tables[0];if(recentBody){recentBody.innerHTML=(data.recent_members||[]).map(m=>`<tr><td><div class="member-cell"><div class="member-avatar">${escapeHtml(initials(m.full_name))}</div><div><div class="member-info-name">${escapeHtml(m.full_name)}</div><div class="member-info-email">${escapeHtml(m.email)}</div></div></div></td><td><span style="font-weight:700;color:var(--color-accent)">${escapeHtml(String(m.plan_title||'NO PLAN').toUpperCase())}</span></td><td>${escapeHtml(formatDate(m.join_date))}</td><td><span class="status-pill ${m.status==='active'?'active':m.status==='suspended'?'danger':'pending'}"><span class="status-dot-sm"></span>${escapeHtml(String(m.status||'unknown').replace(/^./,x=>x.toUpperCase()))}</span></td></tr>`).join('')||'<tr><td colspan="4" style="text-align:center">No members found.</td></tr>';}const expiryList=document.querySelector('.expiry-list');if(expiryList){expiryList.innerHTML=(data.expiry||[]).map(item=>{const days=Number(item.days_left||0),label=days===0?'Today':days===1?'Tomorrow':`In ${days} Days`,cls=days<=1?'danger':'warning';return`<li class="expiry-item"><div><div class="expiry-user-title">${escapeHtml(item.full_name)}</div><div class="expiry-user-sub">${escapeHtml(item.plan_title)} • Expires ${escapeHtml(formatDate(item.end_date))}</div></div><div style="text-align:right"><span class="status-pill ${cls}" style="margin-bottom:.35rem;display:inline-block">${label}</span><div><a href="/admin/memberships.php" style="font-size:.75rem;font-weight:700;color:var(--color-accent)">RENEW</a></div></div></li>`;}).join('')||'<li class="expiry-item">No memberships expire in the next 7 days.</li>';}}
async function loadLiveAdminChart(){const box=document.getElementById('admin-chart-svg');if(!box)return;try{const r=await fetch('/dashboard-chart.php',{credentials:'same-origin',cache:'no-store'});if(!r.ok)return;const d=await r.json();if(!d.success)return;renderAdminChart(box,d.revenue||[],d.attendance||[]);}catch(e){console.warn('IRONCORE chart unavailable:',e);}}
function renderAdminChart(box,revenue,attendance){const active=(box.closest('.chart-section-card')?.querySelector('.switch-btn.active')?.dataset.view)||'revenue';const points=active==='attendance'?attendance:revenue;if(!points.length){box.innerHTML='<div style="padding:2rem;text-align:center;color:var(--color-text-muted)">No chart data available yet.</div>';return;}const width=900,height=280,pad=42,max=Math.max(1,...points.map(p=>Number(p.value)||0)),step=(width-pad*2)/Math.max(1,points.length-1);const coords=points.map((p,i)=>[pad+i*step,height-pad-(Number(p.value)||0)/max*(height-pad*2)]);const path=coords.map((p,i)=>(i?'L':'M')+p[0].toFixed(1)+' '+p[1].toFixed(1)).join(' ');const circles=coords.map((p,i)=>`<circle cx="${p[0]}" cy="${p[1]}" r="4" fill="currentColor"><title>${escapeHtml(points[i].label)}: ${Number(points[i].value).toLocaleString('en-IN')}</title></circle>`).join('');const labels=points.map((p,i)=>`<text x="${coords[i][0]}" y="${height-12}" text-anchor="middle" fill="currentColor" font-size="12">${escapeHtml(p.label)}</text>`).join('');box.innerHTML=`<svg viewBox="0 0 ${width} ${height}" width="100%" height="280" role="img" aria-label="Live ${active} chart"><path d="${path}" fill="none" stroke="currentColor" stroke-width="3" vector-effect="non-scaling-stroke"/><g>${circles}</g><g opacity=".65">${labels}</g></svg>`;box.querySelectorAll('.switch-btn');box.closest('.chart-section-card')?.querySelectorAll('.switch-btn').forEach(btn=>{if(!btn.dataset.liveBound){btn.dataset.liveBound='1';btn.addEventListener('click',()=>setTimeout(loadLiveAdminChart,0));}});}
function updateTrainerDashboard(data){const s=data.stats||{};setKpiByTitle('Assigned Clients',Number(s.assigned_clients||0).toLocaleString('en-IN'));setKpiByTitle('Active Programs',Number(s.active_programs||0).toLocaleString('en-IN'));setKpiByTitle('Sessions',Number(s.sessions||0).toLocaleString('en-IN'));setKpiByTitle('Needs Attention',Number(s.attention||0).toLocaleString('en-IN'));const table=document.querySelector('.data-table tbody');if(table&&data.clients){table.innerHTML=data.clients.map(c=>`<tr><td><div class="member-cell"><div class="member-avatar">${escapeHtml(initials(c.full_name))}</div><div><div class="member-info-name">${escapeHtml(c.full_name)}</div><div class="member-info-email">${escapeHtml(c.email)}</div></div></div></td><td>${escapeHtml(c.plan_title||'No Plan')}</td><td>${escapeHtml(c.last_attendance?formatDate(c.last_attendance):'Never')}</td><td><span class="status-pill ${c.last_attendance?'active':'warning'}"><span class="status-dot-sm"></span>${c.last_attendance?'Active':'Needs attention'}</span></td></tr>`).join('');}}
function updateMemberDashboard(data){const s=data.summary||{};setKpiByTitle('Current Plan',String(s.current_plan||'NO PLAN'));setKpiByTitle('Days Remaining',String(s.days_remaining||0));setKpiByTitle('Workout Streak',`${Number(s.workout_streak||0)} days`);setKpiByTitle('Attendance Rate',`${Number(s.attendance_rate||0)}%`);const progress=data.progress;document.querySelectorAll('[data-progress-value]').forEach(el=>{const key=el.dataset.progressValue;if(progress&&progress[key]!==undefined&&progress[key]!==null)el.textContent=progress[key];});document.querySelectorAll('[data-workout-list]').forEach(list=>{list.innerHTML=(data.workouts||[]).map(w=>`<div class="workout-live-item"><strong>${escapeHtml(w.title)}</strong><span>${escapeHtml(w.goal||'Training program')}${w.trainer_name?` • ${escapeHtml(w.trainer_name)}`:''}</span></div>`).join('')||'<div class="workout-live-item">No workout plans assigned yet.</div>';});}
